namespace Helper
{
    public enum NeighbourDirection
    {
        // those values are used to multiply with the step size and automatically get the correct direction
        Left = 1,
        Right = -1
    }
}